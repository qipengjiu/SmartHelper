# 智能文本助手插件 UI 文本设计图

## 1. 设计目标
- 页面简洁大方：信息层级清晰，减少视觉干扰。
- 方便用户操作：选中即出现，按钮少而明确，一步完成核心任务。
- 反馈即时：翻译与朗读状态清晰可见，避免用户等待焦虑。

## 2. 使用场景与交互流程
1. 用户在网页中选中“关键词”或“一段文字”。
2. 选区附近出现悬浮工具条（按钮顺序固定：翻译、朗读）。
3. 点击“翻译”后，在悬浮条下方展开翻译结果卡片。
4. 用户可切换翻译目标语言（中文 / 英文），结果即时刷新。
5. 点击“朗读”后，调用 Chrome 内置朗读能力并展示状态（朗读中 / 已暂停 / 已结束）。

## 3. 页面结构（文本线框图）

### 3.1 选中文本后的悬浮工具条（默认态）
```
┌──────────────────────────────────────┐
│ [翻译]   [朗读]                      │
└──────────────────────────────────────┘
```

说明：
- 出现位置：紧贴选中文本上方，空间不足时自动显示在下方。
- 宽度：自适应内容；高度紧凑，避免遮挡正文阅读。
- 按钮顺序：翻译在前，朗读在后（严格符合需求）。

### 3.2 点击“翻译”后的展开态
```
┌──────────────────────────────────────┐
│ [翻译]   [朗读]                      │
└──────────────────────────────────────┘
┌──────────────────────────────────────┐
│ 目标语言： (● 中文)  (○ 英文)         │
│                                      │
│ 翻译结果：                            │
│ ------------------------------------ │
│ 这里显示翻译文本内容……               │
│ ------------------------------------ │
│ 状态：翻译完成                        │
└──────────────────────────────────────┘
```

说明：
- 翻译结果紧跟在悬浮条下方，减少视线移动。
- 目标语言默认值：根据当前选中文本语言智能推荐（可手动切换）。
- 切换语言后触发重新翻译并覆盖结果区内容。

### 3.3 点击“朗读”后的状态提示
```
┌──────────────────────────────────────┐
│ [翻译]   [朗读中…]                   │
└──────────────────────────────────────┘
```

可选状态文案：
- 朗读中…
- 已暂停（再次点击继续）
- 朗读结束
- 朗读失败，请重试

## 4. 组件清单
- 悬浮容器（Floating Panel）
- 主操作按钮：翻译按钮、朗读按钮
- 翻译结果卡片（可折叠）
- 语言切换器（中文 / 英文，单选）
- 状态提示区（翻译状态、朗读状态、错误提示）

## 5. 文案规范（建议）

### 5.1 按钮文案
- 翻译
- 朗读
- 朗读中…

### 5.2 状态文案
- 正在翻译…
- 翻译完成
- 翻译失败，请检查网络后重试
- 朗读中…
- 已暂停
- 朗读结束
- 当前页面不支持朗读

## 6. 视觉与交互规范（简洁易用）
- 色彩：中性色为主，主按钮使用单一强调色（例如蓝色）。
- 圆角与阴影：轻微圆角 + 轻阴影，增强悬浮层可辨识度。
- 字体：系统默认字体，保证跨平台一致性。
- 间距：按钮左右留白充足，避免误触。
- 动效：展开结果卡片使用 150~200ms 淡入，保证流畅不突兀。
- 可访问性：按钮最小可点击区域建议不小于 32px。

## 7. 异常与边界场景
- 未选中文本：不显示悬浮工具条。
- 选中文本过短（如单个符号）：提示“请选择有效文本”。
- 接口超时：结果区显示“翻译超时，请重试”。
- 连续多次选中：悬浮条跟随最后一次选区位置，旧结果自动收起。

## 8. 开发落地建议（与需求对齐）
- 翻译能力：接入 Kimi API，按目标语言参数调用。
- 朗读能力：调用 Chrome 内置朗读能力（TTS）。
- 插件形态：内容脚本监听选中事件，渲染浮层 UI。
- README 需补充：安装、权限说明、Kimi API 配置项、Chrome 插件加载步骤。
